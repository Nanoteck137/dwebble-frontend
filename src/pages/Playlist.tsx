const Playlist = () => {
  // const apiClient = useApiClient();

  // const query = createQuery(() => ({
  //   queryKey: ["playlists"],
  //   queryFn: async () => {
  //     const artists = await apiClient.getArtists();
  //     if (artists.status === "error") throw new Error(artists.error.message);

  //     return artists.data;
  //   },
  // }));

  return <p>Playlist Page</p>;
};

export default Playlist;
